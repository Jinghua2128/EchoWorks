{
  "name": "feedback-playbook",
  "version": "1.0.0",
  "private": true,
  "description": "A GitHub Pages-ready feedback training app for Singapore Red Cross managers.",
  "scripts": {
    "check": "node --check assets/js/app.js && node --check assets/js/novel.js"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
